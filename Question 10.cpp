#include <iostream> 
using namespace std;


int main() {  
   int y, z;
    cout << "Enter two integers: ";  
	cin >> x >> z;
    int larger = (x > z) ? x : z;  
	cout << "Larger value: " << larger << endl;
	
	
    return 0;
}
